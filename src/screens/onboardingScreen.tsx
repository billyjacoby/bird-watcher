import {BaseText, BaseView} from '@components';

export const OnBoardingScreen = () => {
  return (
    <BaseView>
      <BaseText className="text-3xl">Onboarding Screen</BaseText>
    </BaseView>
  );
};
